#!/bin/bash

declare -i date_day="$(expr $(date +'%d'))"
declare -i split_day=15

if [ $date_day -lt $split_day ]; then
  echo ~/notes/tasks/$(date +%Y)/$(date +%m)a.md
else
  echo ~/notes/tasks/$(date +%Y)/$(date +%m)b.md
fi
